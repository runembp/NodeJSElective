const canal = {
    location: "Copenhagen" // In JS Objects, it is entirely optional to use quotation marks - 
    // which is not optional in JSON Objects
}

console.log(canal);
console.log(canal.location)     // Dot notation
console.log(canal["location"])  // Square notation